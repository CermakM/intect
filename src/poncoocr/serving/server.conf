model_config_list: {

  config: {
    name: "lowercase",
    base_path: "/data/models/lowercase",
    model_platform: "tensorflow"
  },

  config: {
     name: "uppercase",
     base_path: "/data/models/uppercase",
     model_platform: "tensorflow"
  },

  config: {
     name: "numbers",
     base_path: "/data/models/numbers",
     model_platform: "tensorflow"
  }
}
