version: '3'

services:
    intect_minimal:
        container_name: intect_minimal
        build:
            context: .
            dockerfile: Dockerfile.min
        ports:
            - "6006:6006"
        volumes:
            - .:/code:z
        tty: true
